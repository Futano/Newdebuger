# Sample Artifacts

This directory contains small, sanitized artifacts for reviewers who want to
inspect GUIdebuger output without running an Android device or an LLM endpoint.

The files are intentionally compact. Full experiment logs, screenshots, APKs,
and large report sets should be attached as a release asset or archived in a
separate artifact repository instead of being committed to the source branch.
